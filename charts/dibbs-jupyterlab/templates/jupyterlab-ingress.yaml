---
# K8s ingress access
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .Release.Name }}-ingress"
  labels:
    name: "{{ .Release.Name }}"
spec:
  rules:
    - host: "{{ template "JUPYTERLAB.INGRESSPREFIX" }}.{{ .Values.ingress.hostSuffix }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "{{ .Release.Name }}-svc"
                port:
                  number: {{ .Values.ingress.port }}